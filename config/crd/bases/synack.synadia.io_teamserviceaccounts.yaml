---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: teamserviceaccounts.synack.synadia.io
spec:
  group: synack.synadia.io
  names:
    kind: TeamServiceAccount
    listKind: TeamServiceAccountList
    plural: teamserviceaccounts
    shortNames:
    - tsa
    singular: teamserviceaccount
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .status.id
      name: ServiceAccountID
      type: string
    - jsonPath: .status.teamAppUserId
      name: TeamAppUserID
      type: string
    - jsonPath: .status.teamId
      name: TeamID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TeamServiceAccount is the Schema for team service account resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TeamServiceAccountSpec captures desired state for a team-scoped
              service account.
            properties:
              name:
                description: Name is the display name for the service account.
                type: string
              serviceAccountId:
                description: |-
                  ServiceAccountID is the Control Plane service account ID.
                  If omitted on create the controller will create the service account and backfill the ID in status.
                type: string
              teamId:
                description: TeamID identifies the target Control Plane team ID directly.
                  Mutually exclusive with TeamRef.
                type: string
              teamRef:
                description: |-
                  TeamRef references a Team CR in the same namespace.
                  When set, the resource waits for the Team status.id and uses that ID.
                properties:
                  name:
                    description: Name is the referenced Team resource name in the
                      same namespace.
                    type: string
                required:
                - name
                type: object
              teamRoleId:
                description: TeamRoleID is an optional baseline team-level role for
                  this service account.
                type: string
            required:
            - name
            type: object
          status:
            description: TeamServiceAccountStatus reflects observed state from reconciliation.
            properties:
              id:
                type: string
              lastSynced:
                type: string
              message:
                type: string
              observedGeneration:
                format: int64
                type: integer
              teamAppUserId:
                type: string
              teamId:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
