---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: natsusers.synack.synadia.io
spec:
  group: synack.synadia.io
  names:
    kind: NatsUser
    listKind: NatsUserList
    plural: natsusers
    shortNames:
    - user
    singular: natsuser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.accountId
      name: AccountID
      type: string
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .status.natsUserId
      name: UserID
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NatsUser is the Schema for NATS user resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NatsUserSpec captures desired NATS User behavior in Control
              Plane terms.
            properties:
              accountId:
                description: AccountID identifies the target Control Plane account
                  ID.
                type: string
              accountPublicNKey:
                description: |-
                  AccountPublicNKey identifies the target account by public NKEY.
                  When this is set, SystemID must also be set so lookup can be scoped.
                type: string
              accountRef:
                description: |-
                  AccountRef references an Account CR in the same namespace.
                  When set, the resource waits for the Account status.accountId and uses that ID.
                properties:
                  name:
                    description: Name is the referenced Account resource name in the
                      same namespace.
                    type: string
                required:
                - name
                type: object
              allowedConnectionTypes:
                description: AllowedConnectionTypes restricts connection types for
                  the user.
                items:
                  type: string
                type: array
              bearerToken:
                description: BearerToken enables bearer token mode.
                type: boolean
              data:
                description: Data sets the data limit (bytes, -1 unlimited).
                format: int64
                type: integer
              jwtExpiresInSecs:
                description: JwtExpiresInSecs sets the JWT expiration in seconds.
                format: int64
                type: integer
              name:
                description: Name is the NATS user name.
                type: string
              natsUserId:
                description: NatsUserID optionally pins this resource to an existing
                  Control Plane user ID.
                type: string
              payload:
                description: Payload sets the max message payload size (bytes, -1
                  unlimited).
                format: int64
                type: integer
              signingKeyGroupId:
                default: Default
                description: SigningKeyGroupID is the signing key group to use for
                  this user.
                type: string
              subs:
                description: Subs sets the max subscriptions (-1 unlimited).
                format: int64
                type: integer
              systemId:
                description: SystemID scopes AccountPublicNKey resolution to a specific
                  system.
                type: string
              tags:
                description: Tags are user-defined tags.
                items:
                  type: string
                type: array
            required:
            - name
            type: object
          status:
            description: NatsUserStatus reflects observed state from reconciliation.
            properties:
              accountId:
                type: string
              lastSynced:
                type: string
              message:
                type: string
              natsUserId:
                type: string
              observedGeneration:
                format: int64
                type: integer
              userPublicKey:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
